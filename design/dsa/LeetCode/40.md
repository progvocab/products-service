Problem (Combination Sum II)
Given an array candidates (may contain duplicates) and a target, find all unique combinations that sum to target.
Each number can be used at most once.
Combinations must be unique (no duplicates).
Return all valid combinations.
Order within combinations doesn’t matter.


---

Solution Steps (Backtracking + Sorting):

1. Sort the array to handle duplicates.


2. Use recursive function with index and current sum.


3. If sum == target → add combination to result.


4. If sum > target → backtrack.


5. Iterate from current index to end.


6. Skip duplicates: if i > start && nums[i] == nums[i-1] → continue.


7. Include current element.


8. Recurse with i + 1 (no reuse).


9. Backtrack by removing last element.


10. Continue exploring all possibilities.




---

Pattern:
Backtracking (Subset with Duplicates Handling)


---

Advantage over Brute Force:
Avoids duplicate combinations via sorting.
Prunes invalid paths early.
Efficient subset exploration instead of all permutations.


### 41

Problem (First Missing Positive)
Given an unsorted integer array, find the smallest missing positive integer.
Must run in O(n) time and O(1) extra space.
Array may contain negatives and zeros.
Rearrangement of elements is allowed.
Return the smallest positive integer not present.


---

Solution Steps (Cyclic Sort / Index Mapping):

1. Iterate through array.


2. While nums[i] in range [1, n] and not in correct position:


3. Swap nums[i] with nums[nums[i] - 1].


4. Continue until all valid numbers are placed correctly.


5. Traverse array again.


6. First index i where nums[i] != i+1 → return i+1.


7. If all correct → return n+1.




---

Pattern:
Cyclic Sort / Index Placement


---

Advantage over Brute Force:
Achieves O(n) without extra space.
Avoids sorting O(n log n).
Directly maps values to indices efficiently.


### 42

Problem (Trapping Rain Water)
Given an array height[] representing elevation, compute trapped rainwater.
Water is trapped between taller bars.
Width of each bar is 1.
Return total water trapped.
Array may contain varying heights.


---

Solution Steps (Two Pointer):

1. Initialize left = 0, right = n-1.


2. Track leftMax = 0, rightMax = 0.


3. While left < right:


4. If height[left] < height[right]:


5. If height[left] >= leftMax → update leftMax.


6. Else add leftMax - height[left] to result.


7. Move left++.


8. Else process right side similarly.


9. Move right--.


10. Return total water.




---

Pattern:
Two Pointers (Prefix Max Optimization)


---

Advantage over Brute Force:
Avoids nested scanning O(n²).
Achieves O(n) with constant space.
Efficiently computes water using boundaries.


### 43

Problem (Multiply Strings)
Given two non-negative integers as strings, return their product as a string.
Cannot use built-in big integer libraries.
Numbers can be very large.
Simulate multiplication manually.
Return result without leading zeros.


---

Solution Steps (Simulation / Array Multiplication):

1. Initialize result array of size n + m.


2. Traverse both strings from right to left.


3. Multiply digits num1[i] * num2[j].


4. Add product to result[i + j + 1].


5. Handle carry: add to result[i + j].


6. Repeat for all digit pairs.


7. Convert result array to string.


8. Skip leading zeros.


9. Return final string.




---

Pattern:
Math Simulation (Digit-wise Multiplication)


---

Advantage over Brute Force:
Avoids converting to integers (overflow safe).
Efficient O(n * m) digit operations.
Mimics manual multiplication precisely.


### 44

Problem (Wildcard Matching)
Given a string s and pattern p, implement wildcard matching.
? matches any single character.
* matches any sequence (including empty).
Match must cover the entire string.
Return true if match succeeds.


---

Solution Steps (Greedy + Two Pointer):

1. Initialize pointers i (s), j (p), star = -1, match = 0.


2. If characters match or ? → move both pointers.


3. If * → record star = j, match = i, move j++.


4. If mismatch and star != -1:


5. Backtrack: set j = star + 1.


6. Increment match, set i = match.


7. Else return false.


8. After loop, ensure remaining pattern chars are *.


9. Return true if valid.




---

Pattern:
Greedy + Two Pointers (Backtracking on *)


---

Advantage over Brute Force:
Avoids exponential recursion.
Runs in O(n) average time.
Efficient handling of * via controlled backtracking.

### 45

Problem (Jump Game II)
Given an array nums where each element represents max jump length, find minimum jumps to reach last index.
You start at index 0.
It is guaranteed that you can reach the last index.
Return the minimum number of jumps.
Optimize for linear time.


---

Solution Steps (Greedy / BFS-like):

1. Initialize jumps = 0, currEnd = 0, farthest = 0.


2. Traverse array from index 0 to n-2.


3. Update farthest = max(farthest, i + nums[i]).


4. If i == currEnd:


5. Increment jumps.


6. Set currEnd = farthest.


7. Continue traversal.


8. Return jumps.




---

Pattern:
Greedy (Level-based / BFS idea)


---

Advantage over Brute Force:
Avoids exploring all paths (exponential).
Linear time O(n).
Efficiently tracks reachable ranges.


### 46

Problem (Permutations)
Given an array of distinct integers, return all possible permutations.
Each permutation must contain all elements exactly once.
Return all permutations in any order.
Array size is small (≤ 6 typically).
No duplicates in input.


---

Solution Steps (Backtracking):

1. Initialize result list.


2. Use recursive function with current path.


3. If path size == n → add to result.


4. Iterate through all elements.


5. Skip elements already in current path.


6. Add element to path.


7. Recurse for next position.


8. Backtrack by removing last element.


9. Continue until all permutations generated.




---

Pattern:
Backtracking (Permutation Generation)


---

Advantage over Brute Force:
Systematic generation without redundancy.
Avoids duplicate permutations.
Efficient recursion structure for small n.


### 47

Problem (Permutations II)
Given an array nums that may contain duplicates, return all unique permutations.
Each permutation must include all elements exactly once.
Result should not contain duplicate permutations.
Order of output does not matter.
Array size is small.


---

Solution Steps (Backtracking + Sorting):

1. Sort the array to group duplicates.


2. Use a boolean used[] array.


3. Initialize result list.


4. Recursively build permutations.


5. If path size == n → add to result.


6. Iterate through indices 0 to n-1.


7. Skip if used[i] is true.


8. Skip duplicates: if i > 0 && nums[i] == nums[i-1] && !used[i-1].


9. Mark used, add to path, recurse.


10. Backtrack: unmark and remove element.




---

Pattern:
Backtracking (Permutation with Duplicates Handling)


---

Advantage over Brute Force:
Avoids duplicate permutations efficiently.
Prunes redundant branches early.
More optimal than generating all permutations then filtering.


### 48

Problem (Rotate Image)
Given an n x n matrix, rotate it 90° clockwise in-place.
Do not use extra matrix space.
Modify the input matrix directly.
Rotation must be done layer by layer.
Return the updated matrix.


---

Solution Steps (Transpose + Reverse):

1. Transpose the matrix (swap matrix[i][j] with matrix[j][i]).


2. Iterate only for i < j to avoid double swap.


3. After transpose, reverse each row.


4. Use two pointers to reverse rows in-place.


5. Continue for all rows.


6. Return the modified matrix.




---

Pattern:
Matrix Transformation (Transpose + Reverse)


---

Advantage over Brute Force:
Avoids extra space for new matrix.
In-place → O(1) space.
Efficient O(n²) transformation.


### 59

Problem (Spiral Matrix II)
Given an integer n, generate an n x n matrix filled with numbers from 1 to n² in spiral order.
Start from top-left and move clockwise.
Fill layer by layer.
Return the generated matrix.
All numbers must be used exactly once.


---

Solution Steps (Simulation / Boundary Traversal):

1. Initialize matrix n x n and boundaries: top, bottom, left, right.


2. Initialize num = 1.


3. Traverse left → right across top row.


4. Move top boundary down.


5. Traverse top → bottom along right column.


6. Move right boundary left.


7. Traverse right → left across bottom row.


8. Move bottom boundary up.


9. Traverse bottom → top along left column.


10. Move left boundary right and repeat until filled.




---

Pattern:
Matrix Simulation (Spiral Traversal)


---

Advantage over Brute Force:
Systematic filling without extra checks.
Single pass → O(n²).
Efficient boundary control avoids revisits.


### 49

Problem (Group Anagrams)
Given an array of strings, group the anagrams together.
Anagrams are strings with same characters in different order.
Return groups of anagrams.
Order of output does not matter.
All strings are lowercase.


---

Solution Steps (Hashing + Sorting):

1. Initialize a hashmap.


2. For each string, sort its characters → key.


3. Use sorted string as hashmap key.


4. Append original string to corresponding list.


5. Repeat for all strings.


6. Return all hashmap values as result.




---

Pattern:
Hashing (Anagram Grouping)


---

Advantage over Brute Force:
Avoids pairwise comparison O(n²).
Efficient grouping in O(n * k log k).
Uses canonical representation for matching.